services:
  api:
    build: .
    ports:
      - "8001:8000"
    env_file:
      - .env
    environment:
      - PORT=8000
    volumes:
      - codes-data:/app/data
    restart: unless-stopped

volumes:
  codes-data:
